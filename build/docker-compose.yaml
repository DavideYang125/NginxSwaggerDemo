version: '3.4'

services:
  test_demo.api:
    container_name: demo.api
    build:
      context: ..
      dockerfile: src/NginxDemo.Api/Dockerfile
    ports:
      - '8011:80'
    dns:
      - 8.8.8.8
      - 114.114.114.114